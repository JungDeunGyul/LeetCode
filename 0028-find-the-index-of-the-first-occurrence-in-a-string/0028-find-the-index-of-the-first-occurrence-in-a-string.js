/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
const strStr = function(haystack, needle) {
  const isIndex = haystack.indexOf(needle);
  
  if(isIndex === -1) {
    return isIndex;
  } else {
    return isIndex;
  }
};